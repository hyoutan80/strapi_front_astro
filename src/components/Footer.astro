---
import { Monitor, Smartphone, BookOpen, Heart } from "lucide-react";

const siteMapLinks = [
  { name: "プライバシーポリシー", href: "/privacy" },
  { name: "お問い合わせ", href: "/contact" },
  { name: "免責事項", href: "/disclaimer" },
];

const categoryLinks = [
  { name: "Web制作・開発", href: "/blog/web-development", icon: Monitor },
  { name: "AI・Web3", href: "/blog/ai-web3", icon: Smartphone },
  { name: "ディレクション", href: "/blog/direction", icon: BookOpen },
  { name: "ライフスタイル", href: "/blog/lifestyle", icon: Heart },
];
---

<footer class="w-full border-t border-border/40 bg-zinc-950/80 backdrop-blur-sm text-muted-foreground pt-12 pb-8">
    <div class="container mx-auto px-4 md:px-6">
        <div class="grid grid-cols-1 md:grid-cols-12 gap-12 mb-12 text-sm">
            
            <!-- Logo & Description -->
            <div class="md:col-span-6 space-y-4">
                <div class="flex items-center gap-2 text-foreground">
                    <div class="bg-primary/10 p-1.5 rounded-lg">
                        <span class="text-lg">🚀</span>
                    </div>
                    <div>
                        <h2 class="font-bold text-lg leading-none">Web3Ships</h2>
                        <p class="text-[10px] text-muted-foreground">AI開発とWeb制作の技術メディア</p>
                    </div>
                </div>
                <p class="leading-relaxed text-muted-foreground/80 max-w-sm">
                    次世代のWeb開発から最新のAI・Web3技術まで、<br>
                    明日のキャッチアップに必要な知識をお届けします。
                </p>
            </div>

            <!-- Site Map -->
            <div class="md:col-span-3 space-y-4">
                <h3 class="font-bold text-foreground">サイトマップ</h3>
                <ul class="space-y-2">
                    {siteMapLinks.map(link => (
                        <li key={link.name}>
                            <a href={link.href} class="hover:text-primary transition-colors text-xs">
                                {link.name}
                            </a>
                        </li>
                    ))}
                </ul>
            </div>

            <!-- Categories -->
            <div class="md:col-span-3 space-y-4">
                <h3 class="font-bold text-foreground">カテゴリー</h3>
                <ul class="space-y-2">
                    {categoryLinks.map(link => (
                        <li>
                            <a href={link.href} class="hover:text-primary transition-colors text-xs flex items-center gap-2">
                                <link.icon className="w-3 h-3" />
                                {link.name}
                            </a>
                        </li>
                    ))}
                </ul>
            </div>

        </div>

        <div class="border-t border-border/40 pt-8 text-center">
            <p class="text-[10px] text-muted-foreground">
                (c) 2025 Web3Ships - AI & Web Development Media.
            </p>
        </div>
    </div>
</footer>
